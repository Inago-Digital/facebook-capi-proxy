# TODO

- [x] Add root `pnpm` scripts to run both `server` and `dashboard` together (`dev`, `build`) for easier local workflow
- [x] Add simple pagination or "load more" for Logs to keep UI responsive with larger datasets
- [x] Add a small `GET /version` endpoint in server with app version + environment
- [x] Add user-friendly error messages when login fails (e.g. "Invalid credentials") instead of generic "Unauthorized"
